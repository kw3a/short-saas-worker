# Azure Speech
AZURE_KEY=
AZURE_REGION=

# Bearer token required on incoming /synthesize requests. Must match the
# TTS_SECRET (or VIDEO_SERVER_SECRET) configured in go-video.
TTS_SECRET=change-me

# Optional: port to listen on (default 3001)
PORT=3001
